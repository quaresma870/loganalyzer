[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "loganalyzer"
version = "1.1.1"
description = "Parse, analyse and report on log files"
requires-python = ">=3.11"
dependencies = ["click>=8.1.0", "rich>=13.7.0", "pyyaml>=6.0.1", "schedule>=1.2.0", "requests>=2.31.0"]

[project.optional-dependencies]
geoip = ["geoip2>=4.8.0"]
dashboard = ["fastapi>=0.110.0", "uvicorn>=0.29.0"]

[project.scripts]
loganalyzer = "loganalyzer.cli:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]
